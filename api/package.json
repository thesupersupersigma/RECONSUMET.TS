{
  "name": "anime-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted HTTP API over the anime aggregator (AniList search/mapping + HLS proxy).",
  "scripts": {
    "start": "node src/server.mjs"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0"
  }
}
